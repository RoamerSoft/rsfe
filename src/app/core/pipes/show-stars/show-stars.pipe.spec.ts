import { ShowStarsPipe } from './show-stars.pipe';

describe('ShowStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
